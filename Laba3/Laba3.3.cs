using System;

class Program {
    static void Taylors_Row(double x, double res) {
      res = arctg((Math.Pi / 2) + )
    }

    static void Main() {
        double x, res;
        Console.WriteLine($"Taylors Row:");
        Console.WriteLine("=====================================");
        for (int n = 0; n < 5; n++) {
            Taylors_Row(n, x, res);
        }
        Console.WriteLine("=====================================");
    }
}
